// postcss.config.js
// PostCSS configuration for MindMelt Socratic Learning Platform

module.exports = {
  plugins: [
    // Tailwind CSS - main utility framework
    require('tailwindcss'),
    
    // Autoprefixer - adds vendor prefixes for browser compatibility
    require('autoprefixer')({
      // Browser support configuration
      overrideBrowserslist: [
        '>0.2%',
        'not dead',
        'not op_mini all',
        'last 2 versions',
        'Firefox ESR'
      ],
      // Grid support
      grid: 'autoplace',
      // Remove outdated prefixes
      remove: true
    })
  ]
};

// Simple development logging
if (process.env.NODE_ENV === 'development') {
  console.log('🎨 PostCSS: Development mode with Tailwind + Autoprefixer');
} else if (process.env.NODE_ENV === 'production') {
  console.log('🚀 PostCSS: Production mode with optimizations');
}