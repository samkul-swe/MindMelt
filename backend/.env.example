# Backend Environment Variables

# Firebase Service Account Key (JSON content as string)
FIREBASE_SERVICE_ACCOUNT_KEY=your-filename.json

# API Configuration
PORT=5000
NODE_ENV=development
JWT_SECRET=your_jwt_secret_here_make_it_long_and_secure
JWT_EXPIRES_IN=7d

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# AI API Key Setup
GEMINI_API_KEY=your_key_here

# Instructions:
# 1. Get Firebase Admin SDK credentials from Firebase Console > Project Settings > Service Accounts
# 2. Download the service account JSON file
# 3. Copy the entire JSON content as a string to FIREBASE_SERVICE_ACCOUNT_KEY
# 4. Generate a secure JWT_SECRET for user authentication
# 5. Never commit this file to version control